{
  "name": "footy-tipper-delivery-watchdog",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "check": "node --check src/Code.js && node -e \"JSON.parse(require('fs').readFileSync('src/appsscript.json', 'utf8'))\"",
    "test": "node --test",
    "login": "clasp login",
    "create": "clasp create --type standalone --title \"Footy Tipper Delivery Watchdog\" --rootDir src",
    "push": "clasp push --force",
    "open": "clasp open-script"
  },
  "devDependencies": {
    "@google/clasp": "3.3.0"
  },
  "overrides": {
    "uuid": "11.1.1"
  }
}
