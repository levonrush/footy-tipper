# Runtime dependencies for the footy-tipper pipeline (Docker image + CI).
# Pinned to match the conda env (environment.yml) that trains the models —
# model pickles must be loaded under the same library versions.
# Old kitchen-sink freeze (torch/sagemaker/jupyter/langchain, all unused)
# retired 2026-07; see git history.

# Modelling
numpy==1.26.4
pandas==3.0.1
scipy==1.17.1
scikit-learn==1.8.0
lightgbm==4.6.0
scikit-optimize==0.10.2
dill==0.4.1
joblib==1.5.3

# Scraping (nrl.com lineups + match data) and odds ingestion
requests==2.32.5
beautifulsoup4==4.14.3
lxml==6.0.2
openpyxl==3.1.5

# Config
python-dotenv==1.2.2

# Google Drive / Sheets (send + state sync)
google-api-python-client==2.192.0
google-auth==2.49.0
google-auth-httplib2==0.3.0
gspread==5.12.4

# Email content (LLM copy + banner image)
anthropic>=0.40.0,<1.0.0
openai>=1.0.0,<2.0.0
Pillow==12.1.1
